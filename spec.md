# Lily Design System - Angular Headless - spec

## Status

Placeholder — not yet implemented.

## Plan

When work begins on this subproject, follow the conventions established by the existing six headless libraries:

- Angular 18+ standalone components
- TypeScript
- Component file structure parallels the React headless conventions
- Headless: no CSS, no styles
- WCAG 2.2 AAA accessibility target

## Tasks

- [ ] Decide on Angular version target
- [ ] Set up build pipeline (Vite + Angular adapter, or Angular CLI)
- [ ] Set up test infrastructure (Karma + Jasmine, or Jest + Angular Testing Utilities)
- [ ] Set up Storybook for Angular
- [ ] Generate the 487-component scaffold mirroring the existing headless libraries
- [ ] Wire the example app subproject (`lily-design-system-angular-examples` — also currently a placeholder if needed)
