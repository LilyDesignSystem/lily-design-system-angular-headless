# PostalCodeView

a read-only display of a postal or ZIP code

This is the Angular headless implementation. See `components/postal-code-view/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-postal-code-view></lily-postal-code-view>
```

## Files

- `PostalCodeView.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `PostalCodeView.spec.ts` — vitest + TestBed render test
