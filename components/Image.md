# Image

an image element with alt text

This is the Angular headless implementation. See `components/image/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-image></lily-image>
```

## Files

- `Image.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Image.spec.ts` — vitest + TestBed render test
