# NetPromoterScoreView

a read-only display of a Net Promoter Score

This is the Angular headless implementation. See `components/net-promoter-score-view/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-net-promoter-score-view></lily-net-promoter-score-view>
```

## Files

- `NetPromoterScoreView.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `NetPromoterScoreView.spec.ts` — vitest + TestBed render test
