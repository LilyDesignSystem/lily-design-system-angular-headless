# Sonner

a toast notification manager

This is the Angular headless implementation. See `components/sonner/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-sonner></lily-sonner>
```

## Files

- `Sonner.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Sonner.spec.ts` — vitest + TestBed render test
