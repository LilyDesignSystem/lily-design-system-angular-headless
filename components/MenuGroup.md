# MenuGroup

a labeled section of menu items within a menu

This is the Angular headless implementation. See `components/menu-group/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-menu-group></lily-menu-group>
```

## Files

- `MenuGroup.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `MenuGroup.spec.ts` — vitest + TestBed render test
