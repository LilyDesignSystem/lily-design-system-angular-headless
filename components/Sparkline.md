# Sparkline

a small inline chart showing a data trend

This is the Angular headless implementation. See `components/sparkline/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-sparkline></lily-sparkline>
```

## Files

- `Sparkline.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Sparkline.spec.ts` — vitest + TestBed render test
