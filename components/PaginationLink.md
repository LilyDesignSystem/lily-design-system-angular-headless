# PaginationLink

one pagination link in the trail

This is the Angular headless implementation. See `components/pagination-link/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-pagination-link></lily-pagination-link>
```

## Files

- `PaginationLink.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `PaginationLink.spec.ts` — vitest + TestBed render test
