# AccordionLink

an accordion link

This is the Angular headless implementation. See `components/accordion-link/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-accordion-link></lily-accordion-link>
```

## Files

- `AccordionLink.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `AccordionLink.spec.ts` — vitest + TestBed render test
