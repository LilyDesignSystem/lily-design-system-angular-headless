# PostalCodeInput

an input for entering a postal or ZIP code

This is the Angular headless implementation. See `components/postal-code-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-postal-code-input></lily-postal-code-input>
```

## Files

- `PostalCodeInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `PostalCodeInput.spec.ts` — vitest + TestBed render test
