# ThemeSelectOption

one option in a theme select dropdown

This is the Angular headless implementation. See `components/theme-select-option/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-theme-select-option></lily-theme-select-option>
```

## Files

- `ThemeSelectOption.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ThemeSelectOption.spec.ts` — vitest + TestBed render test
