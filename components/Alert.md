# Alert

a status message for important information or feedback

This is the Angular headless implementation. See `components/alert/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-alert></lily-alert>
```

## Files

- `Alert.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Alert.spec.ts` — vitest + TestBed render test
