# PaginationList

an ordered list of pagination list items

This is the Angular headless implementation. See `components/pagination-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-pagination-list></lily-pagination-list>
```

## Files

- `PaginationList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `PaginationList.spec.ts` — vitest + TestBed render test
