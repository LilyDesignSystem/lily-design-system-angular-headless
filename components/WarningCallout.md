# WarningCallout

a callout box highlighting a warning message

This is the Angular headless implementation. See `components/warning-callout/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-warning-callout></lily-warning-callout>
```

## Files

- `WarningCallout.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `WarningCallout.spec.ts` — vitest + TestBed render test
