# AiLabel

an indicator of AI instances that is a pathway to AI explainability

This is the Angular headless implementation. See `components/ai-label/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-ai-label></lily-ai-label>
```

## Files

- `AiLabel.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `AiLabel.spec.ts` — vitest + TestBed render test
