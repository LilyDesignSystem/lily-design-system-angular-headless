# Dialog

a modal or non-modal dialog window

This is the Angular headless implementation. See `components/dialog/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-dialog></lily-dialog>
```

## Files

- `Dialog.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Dialog.spec.ts` — vitest + TestBed render test
