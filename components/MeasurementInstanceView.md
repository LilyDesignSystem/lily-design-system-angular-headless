# MeasurementInstanceView

a read-only display of a measurement value and unit

This is the Angular headless implementation. See `components/measurement-instance-view/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-measurement-instance-view></lily-measurement-instance-view>
```

## Files

- `MeasurementInstanceView.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `MeasurementInstanceView.spec.ts` — vitest + TestBed render test
