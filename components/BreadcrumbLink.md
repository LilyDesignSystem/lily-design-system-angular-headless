# BreadcrumbLink

one breadcrumb link in the trail

This is the Angular headless implementation. See `components/breadcrumb-link/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-breadcrumb-link></lily-breadcrumb-link>
```

## Files

- `BreadcrumbLink.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `BreadcrumbLink.spec.ts` — vitest + TestBed render test
