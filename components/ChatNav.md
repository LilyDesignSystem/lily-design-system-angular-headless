# ChatNav

a navigation container for chat information

This is the Angular headless implementation. See `components/chat-nav/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-chat-nav></lily-chat-nav>
```

## Files

- `ChatNav.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ChatNav.spec.ts` — vitest + TestBed render test
