# DontList

a guideline list of discouraged dont-list-item components

This is the Angular headless implementation. See `components/dont-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-dont-list></lily-dont-list>
```

## Files

- `DontList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `DontList.spec.ts` — vitest + TestBed render test
