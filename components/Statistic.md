# Statistic

a numeric value display with title, prefix, and suffix

This is the Angular headless implementation. See `components/statistic/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-statistic></lily-statistic>
```

## Files

- `Statistic.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Statistic.spec.ts` — vitest + TestBed render test
