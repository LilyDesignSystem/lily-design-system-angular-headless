# TimelineList

an ordered list of chronological events or milestones

This is the Angular headless implementation. See `components/timeline-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-timeline-list></lily-timeline-list>
```

## Files

- `TimelineList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TimelineList.spec.ts` — vitest + TestBed render test
