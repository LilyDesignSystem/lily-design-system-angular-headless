# ToolBarButton

one action button in a tool bar

This is the Angular headless implementation. See `components/tool-bar-button/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-tool-bar-button></lily-tool-bar-button>
```

## Files

- `ToolBarButton.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ToolBarButton.spec.ts` — vitest + TestBed render test
