# TextInput

a single-line text input field <input type="text">

This is the Angular headless implementation. See `components/text-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-text-input></lily-text-input>
```

## Files

- `TextInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TextInput.spec.ts` — vitest + TestBed render test
