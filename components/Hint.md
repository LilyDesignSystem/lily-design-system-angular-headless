# Hint

hint text providing guidance for a form field

This is the Angular headless implementation. See `components/hint/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-hint></lily-hint>
```

## Files

- `Hint.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Hint.spec.ts` — vitest + TestBed render test
