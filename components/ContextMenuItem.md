# ContextMenuItem

one item in a context menu

This is the Angular headless implementation. See `components/context-menu-item/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-context-menu-item></lily-context-menu-item>
```

## Files

- `ContextMenuItem.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ContextMenuItem.spec.ts` — vitest + TestBed render test
