# RadioInput

a single radio button input <input type="radio">

This is the Angular headless implementation. See `components/radio-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-radio-input></lily-radio-input>
```

## Files

- `RadioInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `RadioInput.spec.ts` — vitest + TestBed render test
