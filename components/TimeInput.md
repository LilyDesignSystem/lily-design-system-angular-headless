# TimeInput

an input for entering a time value <input type="time">

This is the Angular headless implementation. See `components/time-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-time-input></lily-time-input>
```

## Files

- `TimeInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TimeInput.spec.ts` — vitest + TestBed render test
