# FileInput

an input for selecting files from the file system <input type="file">

This is the Angular headless implementation. See `components/file-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-file-input></lily-file-input>
```

## Files

- `FileInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `FileInput.spec.ts` — vitest + TestBed render test
