# Event

an event component that contains other event-related information

This is the Angular headless implementation. See `components/event/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-event></lily-event>
```

## Files

- `Event.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Event.spec.ts` — vitest + TestBed render test
