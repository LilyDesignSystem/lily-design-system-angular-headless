# Byline

an author attribution with publish and update timestamps

This is the Angular headless implementation. See `components/byline/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-byline></lily-byline>
```

## Files

- `Byline.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Byline.spec.ts` — vitest + TestBed render test
