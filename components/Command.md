# Command

a command palette for searching and executing actions

This is the Angular headless implementation. See `components/command/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-command></lily-command>
```

## Files

- `Command.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Command.spec.ts` — vitest + TestBed render test
