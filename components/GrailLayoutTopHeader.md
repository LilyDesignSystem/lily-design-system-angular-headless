# GrailLayoutTopHeader

grail layout top header full width with <header> tag

This is the Angular headless implementation. See `components/grail-layout-top-header/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-grail-layout-top-header></lily-grail-layout-top-header>
```

## Files

- `GrailLayoutTopHeader.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `GrailLayoutTopHeader.spec.ts` — vitest + TestBed render test
