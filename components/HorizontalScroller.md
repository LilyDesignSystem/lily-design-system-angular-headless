# HorizontalScroller

a horizontally scrollable content container

This is the Angular headless implementation. See `components/horizontal-scroller/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-horizontal-scroller></lily-horizontal-scroller>
```

## Files

- `HorizontalScroller.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `HorizontalScroller.spec.ts` — vitest + TestBed render test
