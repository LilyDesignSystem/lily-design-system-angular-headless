# HamburgerMenu

a toggle button that opens a mobile navigation menu

This is the Angular headless implementation. See `components/hamburger-menu/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-hamburger-menu></lily-hamburger-menu>
```

## Files

- `HamburgerMenu.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `HamburgerMenu.spec.ts` — vitest + TestBed render test
