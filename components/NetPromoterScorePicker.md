# NetPromoterScorePicker

a picker for selecting a 0-10 Net Promoter Score

This is the Angular headless implementation. See `components/net-promoter-score-picker/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-net-promoter-score-picker></lily-net-promoter-score-picker>
```

## Files

- `NetPromoterScorePicker.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `NetPromoterScorePicker.spec.ts` — vitest + TestBed render test
