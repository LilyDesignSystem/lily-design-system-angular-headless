# CareCard

a medical care instruction card with urgency levels

This is the Angular headless implementation. See `components/care-card/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-care-card></lily-care-card>
```

## Files

- `CareCard.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `CareCard.spec.ts` — vitest + TestBed render test
