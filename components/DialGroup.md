# DialGroup

a group of dial components

This is the Angular headless implementation. See `components/dial-group/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-dial-group></lily-dial-group>
```

## Files

- `DialGroup.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `DialGroup.spec.ts` — vitest + TestBed render test
