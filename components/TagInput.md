# TagInput

an input for adding and removing tags

This is the Angular headless implementation. See `components/tag-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-tag-input></lily-tag-input>
```

## Files

- `TagInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TagInput.spec.ts` — vitest + TestBed render test
