# MenuBar

a horizontal bar of menu triggers

This is the Angular headless implementation. See `components/menu-bar/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-menu-bar></lily-menu-bar>
```

## Files

- `MenuBar.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `MenuBar.spec.ts` — vitest + TestBed render test
