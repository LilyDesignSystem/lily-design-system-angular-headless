# AccordionList

an accordion ordered list of list item components

This is the Angular headless implementation. See `components/accordion-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-accordion-list></lily-accordion-list>
```

## Files

- `AccordionList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `AccordionList.spec.ts` — vitest + TestBed render test
