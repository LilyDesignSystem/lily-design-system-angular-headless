# Blockquote

a block-level quotation with optional source citation

This is the Angular headless implementation. See `components/blockquote/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-blockquote></lily-blockquote>
```

## Files

- `Blockquote.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Blockquote.spec.ts` — vitest + TestBed render test
