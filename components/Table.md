# Table

a table with rows and columns <table>

This is the Angular headless implementation. See `components/table/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-table></lily-table>
```

## Files

- `Table.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Table.spec.ts` — vitest + TestBed render test
