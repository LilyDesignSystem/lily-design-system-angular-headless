# MeasurementUnitView

a read-only display of a measurement unit

This is the Angular headless implementation. See `components/measurement-unit-view/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-measurement-unit-view></lily-measurement-unit-view>
```

## Files

- `MeasurementUnitView.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `MeasurementUnitView.spec.ts` — vitest + TestBed render test
