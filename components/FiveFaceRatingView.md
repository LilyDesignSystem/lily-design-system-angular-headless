# FiveFaceRatingView

a read-only display of a five-face satisfaction rating

This is the Angular headless implementation. See `components/five-face-rating-view/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-five-face-rating-view></lily-five-face-rating-view>
```

## Files

- `FiveFaceRatingView.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `FiveFaceRatingView.spec.ts` — vitest + TestBed render test
