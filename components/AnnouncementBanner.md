# AnnouncementBanner

a banner highlighting important messages for all users

This is the Angular headless implementation. See `components/announcement-banner/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-announcement-banner></lily-announcement-banner>
```

## Files

- `AnnouncementBanner.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `AnnouncementBanner.spec.ts` — vitest + TestBed render test
