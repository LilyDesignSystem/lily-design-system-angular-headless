# MeasurementUnitInput

an input for selecting a measurement unit

This is the Angular headless implementation. See `components/measurement-unit-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-measurement-unit-input></lily-measurement-unit-input>
```

## Files

- `MeasurementUnitInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `MeasurementUnitInput.spec.ts` — vitest + TestBed render test
