# DateField

a structured field for entering date components

This is the Angular headless implementation. See `components/date-field/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-date-field></lily-date-field>
```

## Files

- `DateField.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `DateField.spec.ts` — vitest + TestBed render test
