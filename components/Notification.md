# Notification

a brief message about an event or update

This is the Angular headless implementation. See `components/notification/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-notification></lily-notification>
```

## Files

- `Notification.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Notification.spec.ts` — vitest + TestBed render test
