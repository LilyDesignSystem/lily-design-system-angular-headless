# SverigePersonnummerView

a read-only display of Sweden's Personnummer

This is the Angular headless implementation. See `components/sverige-personnummer-view/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-sverige-personnummer-view></lily-sverige-personnummer-view>
```

## Files

- `SverigePersonnummerView.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `SverigePersonnummerView.spec.ts` — vitest + TestBed render test
