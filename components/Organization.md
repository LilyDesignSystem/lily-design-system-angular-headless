# Organization

an organization component that contains other organization-related information

This is the Angular headless implementation. See `components/organization/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-organization></lily-organization>
```

## Files

- `Organization.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Organization.spec.ts` — vitest + TestBed render test
