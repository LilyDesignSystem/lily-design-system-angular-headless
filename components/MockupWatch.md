# MockupWatch

a box area that looks like a smart watch

This is the Angular headless implementation. See `components/mockup-watch/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-mockup-watch></lily-mockup-watch>
```

## Files

- `MockupWatch.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `MockupWatch.spec.ts` — vitest + TestBed render test
