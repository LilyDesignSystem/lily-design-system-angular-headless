# ErrorMessage

an error message associated with a form field

This is the Angular headless implementation. See `components/error-message/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-error-message></lily-error-message>
```

## Files

- `ErrorMessage.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ErrorMessage.spec.ts` — vitest + TestBed render test
