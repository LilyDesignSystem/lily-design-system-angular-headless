# Header

a page or section header area

This is the Angular headless implementation. See `components/header/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-header></lily-header>
```

## Files

- `Header.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Header.spec.ts` — vitest + TestBed render test
