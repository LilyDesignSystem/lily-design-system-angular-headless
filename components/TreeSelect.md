# TreeSelect

a select dropdown showing a tree of hierarchical options

This is the Angular headless implementation. See `components/tree-select/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-tree-select></lily-tree-select>
```

## Files

- `TreeSelect.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TreeSelect.spec.ts` — vitest + TestBed render test
