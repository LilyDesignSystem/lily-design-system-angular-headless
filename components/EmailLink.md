# EmailLink

a mailto hyperlink for an email address

This is the Angular headless implementation. See `components/email-link/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-email-link></lily-email-link>
```

## Files

- `EmailLink.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `EmailLink.spec.ts` — vitest + TestBed render test
