# TourList

an ordered list of tour guide steps

This is the Angular headless implementation. See `components/tour-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-tour-list></lily-tour-list>
```

## Files

- `TourList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TourList.spec.ts` — vitest + TestBed render test
