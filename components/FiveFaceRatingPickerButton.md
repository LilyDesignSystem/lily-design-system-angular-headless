# FiveFaceRatingPickerButton

a picker button for selecting a 1-5 satisfaction rating using face labels

This is the Angular headless implementation. See `components/five-face-rating-picker-button/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-five-face-rating-picker-button></lily-five-face-rating-picker-button>
```

## Files

- `FiveFaceRatingPickerButton.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `FiveFaceRatingPickerButton.spec.ts` — vitest + TestBed render test
