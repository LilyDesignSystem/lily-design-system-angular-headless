# ProgressBar

a horizontal progress indicator

This is the Angular headless implementation. See `components/progress-bar/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-progress-bar></lily-progress-bar>
```

## Files

- `ProgressBar.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ProgressBar.spec.ts` — vitest + TestBed render test
