# Menu

a list of actions or options triggered by a button

This is the Angular headless implementation. See `components/menu/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-menu></lily-menu>
```

## Files

- `Menu.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Menu.spec.ts` — vitest + TestBed render test
