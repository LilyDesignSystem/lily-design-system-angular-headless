# Flair

a decorative highlight or emphasis element

This is the Angular headless implementation. See `components/flair/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-flair></lily-flair>
```

## Files

- `Flair.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Flair.spec.ts` — vitest + TestBed render test
