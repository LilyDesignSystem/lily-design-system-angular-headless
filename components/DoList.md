# DoList

a guideline list of encouraged do-list-item components

This is the Angular headless implementation. See `components/do-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-do-list></lily-do-list>
```

## Files

- `DoList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `DoList.spec.ts` — vitest + TestBed render test
