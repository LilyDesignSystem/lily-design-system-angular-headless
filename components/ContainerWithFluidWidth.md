# ContainerWithFluidWidth

a full-width content wrapper with horizontal padding

This is the Angular headless implementation. See `components/container-with-fluid-width/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-container-with-fluid-width></lily-container-with-fluid-width>
```

## Files

- `ContainerWithFluidWidth.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ContainerWithFluidWidth.spec.ts` — vitest + TestBed render test
