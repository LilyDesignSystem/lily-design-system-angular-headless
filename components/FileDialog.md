# FileDialog

a dialog for browsing and selecting files

This is the Angular headless implementation. See `components/file-dialog/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-file-dialog></lily-file-dialog>
```

## Files

- `FileDialog.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `FileDialog.spec.ts` — vitest + TestBed render test
