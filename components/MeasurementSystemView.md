# MeasurementSystemView

a read-only display of a measurement system

This is the Angular headless implementation. See `components/measurement-system-view/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-measurement-system-view></lily-measurement-system-view>
```

## Files

- `MeasurementSystemView.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `MeasurementSystemView.spec.ts` — vitest + TestBed render test
