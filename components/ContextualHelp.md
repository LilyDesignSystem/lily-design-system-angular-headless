# ContextualHelp

a help button that opens a popover with explanatory content

This is the Angular headless implementation. See `components/contextual-help/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-contextual-help></lily-contextual-help>
```

## Files

- `ContextualHelp.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ContextualHelp.spec.ts` — vitest + TestBed render test
