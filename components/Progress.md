# Progress

a horizontal progress bar showing completion

This is the Angular headless implementation. See `components/progress/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-progress></lily-progress>
```

## Files

- `Progress.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Progress.spec.ts` — vitest + TestBed render test
