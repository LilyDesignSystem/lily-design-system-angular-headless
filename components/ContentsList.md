# ContentsList

an contents ordered list of contents list item components

This is the Angular headless implementation. See `components/contents-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-contents-list></lily-contents-list>
```

## Files

- `ContentsList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ContentsList.spec.ts` — vitest + TestBed render test
