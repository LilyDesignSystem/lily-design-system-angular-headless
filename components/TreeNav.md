# TreeNav

a hierarchical navigation with expandable branches

This is the Angular headless implementation. See `components/tree-nav/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-tree-nav></lily-tree-nav>
```

## Files

- `TreeNav.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TreeNav.spec.ts` — vitest + TestBed render test
