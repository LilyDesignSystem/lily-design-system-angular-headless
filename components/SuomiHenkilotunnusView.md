# SuomiHenkilotunnusView

a read-only display of Finland's Henkilötunnus (HETU)

This is the Angular headless implementation. See `components/suomi-henkilotunnus-view/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-suomi-henkilotunnus-view></lily-suomi-henkilotunnus-view>
```

## Files

- `SuomiHenkilotunnusView.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `SuomiHenkilotunnusView.spec.ts` — vitest + TestBed render test
