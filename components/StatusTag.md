# StatusTag

a tag showing the current status of a task

This is the Angular headless implementation. See `components/status-tag/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-status-tag></lily-status-tag>
```

## Files

- `StatusTag.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `StatusTag.spec.ts` — vitest + TestBed render test
