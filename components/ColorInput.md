# ColorInput

an input for selecting a color value <input type="color">

This is the Angular headless implementation. See `components/color-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-color-input></lily-color-input>
```

## Files

- `ColorInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ColorInput.spec.ts` — vitest + TestBed render test
