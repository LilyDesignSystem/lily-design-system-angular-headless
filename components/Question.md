# Question

a question is anything that asks for information, invites a response, tests knowledge, etc.

This is the Angular headless implementation. See `components/question/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-question></lily-question>
```

## Files

- `Question.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Question.spec.ts` — vitest + TestBed render test
