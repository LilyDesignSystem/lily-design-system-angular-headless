# CheckList

an ordered list of check list item components

This is the Angular headless implementation. See `components/check-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-check-list></lily-check-list>
```

## Files

- `CheckList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `CheckList.spec.ts` — vitest + TestBed render test
