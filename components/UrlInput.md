# UrlInput

an input for entering a URL <input type="url">

This is the Angular headless implementation. See `components/url-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-url-input></lily-url-input>
```

## Files

- `UrlInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `UrlInput.spec.ts` — vitest + TestBed render test
