# SverigePersonnummerInput

an input for entering Sweden's Personnummer

This is the Angular headless implementation. See `components/sverige-personnummer-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-sverige-personnummer-input></lily-sverige-personnummer-input>
```

## Files

- `SverigePersonnummerInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `SverigePersonnummerInput.spec.ts` — vitest + TestBed render test
