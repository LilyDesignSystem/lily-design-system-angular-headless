# Badge

a small label for counts, statuses, or categories

This is the Angular headless implementation. See `components/badge/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-badge></lily-badge>
```

## Files

- `Badge.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Badge.spec.ts` — vitest + TestBed render test
