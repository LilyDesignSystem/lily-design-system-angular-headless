# BreadcrumbList

an ordered list of breadcrumb list items

This is the Angular headless implementation. See `components/breadcrumb-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-breadcrumb-list></lily-breadcrumb-list>
```

## Files

- `BreadcrumbList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `BreadcrumbList.spec.ts` — vitest + TestBed render test
