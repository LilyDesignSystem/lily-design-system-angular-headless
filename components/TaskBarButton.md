# TaskBarButton

one item in a task bar

This is the Angular headless implementation. See `components/task-bar-button/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-task-bar-button></lily-task-bar-button>
```

## Files

- `TaskBarButton.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TaskBarButton.spec.ts` — vitest + TestBed render test
