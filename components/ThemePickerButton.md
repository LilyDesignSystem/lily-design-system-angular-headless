# ThemeSelectButton

a picker button for selecting a visual theme

This is the Angular headless implementation. See `components/theme-select-button/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-theme-select-button></lily-theme-select-button>
```

## Files

- `ThemeSelectButton.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ThemeSelectButton.spec.ts` — vitest + TestBed render test
