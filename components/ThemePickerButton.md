# ThemePickerButton

a picker button for selecting a visual theme

This is the Angular headless implementation. See `components/theme-picker-button/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-theme-picker-button></lily-theme-picker-button>
```

## Files

- `ThemePickerButton.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ThemePickerButton.spec.ts` — vitest + TestBed render test
