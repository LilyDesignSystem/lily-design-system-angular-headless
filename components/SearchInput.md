# SearchInput

an input for entering a search query <input type="search">

This is the Angular headless implementation. See `components/search-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-search-input></lily-search-input>
```

## Files

- `SearchInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `SearchInput.spec.ts` — vitest + TestBed render test
