# DateInput

an input for entering a date value <input type="date">

This is the Angular headless implementation. See `components/date-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-date-input></lily-date-input>
```

## Files

- `DateInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `DateInput.spec.ts` — vitest + TestBed render test
