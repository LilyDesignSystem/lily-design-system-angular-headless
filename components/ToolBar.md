# ToolBar

a horizontal bar of tool actions

This is the Angular headless implementation. See `components/tool-bar/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-tool-bar></lily-tool-bar>
```

## Files

- `ToolBar.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ToolBar.spec.ts` — vitest + TestBed render test
