# Diff

a side-by-side comparison of two items

This is the Angular headless implementation. See `components/diff/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-diff></lily-diff>
```

## Files

- `Diff.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Diff.spec.ts` — vitest + TestBed render test
