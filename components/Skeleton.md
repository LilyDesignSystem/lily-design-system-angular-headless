# Skeleton

a placeholder loading animation for content

This is the Angular headless implementation. See `components/skeleton/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-skeleton></lily-skeleton>
```

## Files

- `Skeleton.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Skeleton.spec.ts` — vitest + TestBed render test
