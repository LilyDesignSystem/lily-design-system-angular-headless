# GrailLayoutCenterMain

grail layout center main content with <main> tag

This is the Angular headless implementation. See `components/grail-layout-center-main/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-grail-layout-center-main></lily-grail-layout-center-main>
```

## Files

- `GrailLayoutCenterMain.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `GrailLayoutCenterMain.spec.ts` — vitest + TestBed render test
