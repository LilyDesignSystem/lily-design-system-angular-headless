# ProgressCircle

a circular progress indicator

This is the Angular headless implementation. See `components/progress-circle/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-progress-circle></lily-progress-circle>
```

## Files

- `ProgressCircle.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ProgressCircle.spec.ts` — vitest + TestBed render test
