# TelLink

a tel hyperlink for a telephone number

This is the Angular headless implementation. See `components/tel-link/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-tel-link></lily-tel-link>
```

## Files

- `TelLink.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TelLink.spec.ts` — vitest + TestBed render test
