# AngleSliderRangeInput

a range input for selecting an angle in degrees

This is the Angular headless implementation. See `components/angle-slider-range-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-angle-slider-range-input></lily-angle-slider-range-input>
```

## Files

- `AngleSliderRangeInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `AngleSliderRangeInput.spec.ts` — vitest + TestBed render test
