# SectionLink

one section link in the navigation

This is the Angular headless implementation. See `components/section-link/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-section-link></lily-section-link>
```

## Files

- `SectionLink.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `SectionLink.spec.ts` — vitest + TestBed render test
