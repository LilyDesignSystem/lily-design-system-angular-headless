# Tag

a keyword label for categorizing content

This is the Angular headless implementation. See `components/tag/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-tag></lily-tag>
```

## Files

- `Tag.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Tag.spec.ts` — vitest + TestBed render test
