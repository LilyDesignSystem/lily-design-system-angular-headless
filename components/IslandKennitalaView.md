# IslandKennitalaView

a read-only display of Iceland's Kennitala

This is the Angular headless implementation. See `components/island-kennitala-view/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-island-kennitala-view></lily-island-kennitala-view>
```

## Files

- `IslandKennitalaView.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `IslandKennitalaView.spec.ts` — vitest + TestBed render test
