# Timer

a countdown or elapsed time display

This is the Angular headless implementation. See `components/timer/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-timer></lily-timer>
```

## Files

- `Timer.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Timer.spec.ts` — vitest + TestBed render test
