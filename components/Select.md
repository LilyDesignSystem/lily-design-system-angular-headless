# Select

a dropdown select element for choosing one option

This is the Angular headless implementation. See `components/select/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-select></lily-select>
```

## Files

- `Select.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Select.spec.ts` — vitest + TestBed render test
