# SummaryList

an ordered list of key-value summary pairs

This is the Angular headless implementation. See `components/summary-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-summary-list></lily-summary-list>
```

## Files

- `SummaryList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `SummaryList.spec.ts` — vitest + TestBed render test
