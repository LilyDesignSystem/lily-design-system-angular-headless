# SlovenskoPasView

a read-only display of Slovakia's Pas

This is the Angular headless implementation. See `components/slovensko-pas-view/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-slovensko-pas-view></lily-slovensko-pas-view>
```

## Files

- `SlovenskoPasView.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `SlovenskoPasView.spec.ts` — vitest + TestBed render test
