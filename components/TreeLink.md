# TreeLink

one link in the tree

This is the Angular headless implementation. See `components/tree-link/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-tree-link></lily-tree-link>
```

## Files

- `TreeLink.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TreeLink.spec.ts` — vitest + TestBed render test
