# DanmarkPersonnummerInput

an input for entering Denmark's Personnummer (CPR-nummer) (CPR)

This is the Angular headless implementation. See `components/danmark-personnummer-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-danmark-personnummer-input></lily-danmark-personnummer-input>
```

## Files

- `DanmarkPersonnummerInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `DanmarkPersonnummerInput.spec.ts` — vitest + TestBed render test
