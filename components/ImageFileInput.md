# ImageFileInput

an input for selecting image files with preview

This is the Angular headless implementation. See `components/image-file-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-image-file-input></lily-image-file-input>
```

## Files

- `ImageFileInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ImageFileInput.spec.ts` — vitest + TestBed render test
