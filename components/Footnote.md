# Footnote

a footnote reference and content element

This is the Angular headless implementation. See `components/footnote/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-footnote></lily-footnote>
```

## Files

- `Footnote.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Footnote.spec.ts` — vitest + TestBed render test
