# CalendarTable

a calendar table interactive grid for managing dates, days, etc. <table>

This is the Angular headless implementation. See `components/calendar-table/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-calendar-table></lily-calendar-table>
```

## Files

- `CalendarTable.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `CalendarTable.spec.ts` — vitest + TestBed render test
