# TreeMenu

a hierarchical tree menu with expandable branches

This is the Angular headless implementation. See `components/tree-menu/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-tree-menu></lily-tree-menu>
```

## Files

- `TreeMenu.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TreeMenu.spec.ts` — vitest + TestBed render test
