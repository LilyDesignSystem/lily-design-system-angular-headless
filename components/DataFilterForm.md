# DataFilterForm

a form for filtering data by criteria

This is the Angular headless implementation. See `components/data-filter-form/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-data-filter-form></lily-data-filter-form>
```

## Files

- `DataFilterForm.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `DataFilterForm.spec.ts` — vitest + TestBed render test
