# ActionBarButton

one action button inside an action bar

This is the Angular headless implementation. See `components/action-bar-button/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-action-bar-button></lily-action-bar-button>
```

## Files

- `ActionBarButton.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ActionBarButton.spec.ts` — vitest + TestBed render test
