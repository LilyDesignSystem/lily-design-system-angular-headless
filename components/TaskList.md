# TaskList

an ordered list of task list items

This is the Angular headless implementation. See `components/task-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-task-list></lily-task-list>
```

## Files

- `TaskList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TaskList.spec.ts` — vitest + TestBed render test
