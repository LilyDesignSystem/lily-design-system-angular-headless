# Scroller

a scrollytelling container with step-based foreground and background composition

This is the Angular headless implementation. See `components/scroller/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-scroller></lily-scroller>
```

## Files

- `Scroller.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Scroller.spec.ts` — vitest + TestBed render test
