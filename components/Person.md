# Person

a person component that contains other person-related information

This is the Angular headless implementation. See `components/person/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-person></lily-person>
```

## Files

- `Person.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Person.spec.ts` — vitest + TestBed render test
