# TimeoutDialog

a modal dialog warning users before session timeout

This is the Angular headless implementation. See `components/timeout-dialog/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-timeout-dialog></lily-timeout-dialog>
```

## Files

- `TimeoutDialog.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TimeoutDialog.spec.ts` — vitest + TestBed render test
