# IconList

an ordered list of icon list item components

This is the Angular headless implementation. See `components/icon-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-icon-list></lily-icon-list>
```

## Files

- `IconList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `IconList.spec.ts` — vitest + TestBed render test
