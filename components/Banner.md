# Banner

a prominent message bar across the top of a page

This is the Angular headless implementation. See `components/banner/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-banner></lily-banner>
```

## Files

- `Banner.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Banner.spec.ts` — vitest + TestBed render test
