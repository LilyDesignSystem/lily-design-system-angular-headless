# Footer

a page or section footer area

This is the Angular headless implementation. See `components/footer/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-footer></lily-footer>
```

## Files

- `Footer.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Footer.spec.ts` — vitest + TestBed render test
