# NetPromoterScorePickerButton

a picker button for selecting a 0-10 Net Promoter Score

This is the Angular headless implementation. See `components/net-promoter-score-picker-button/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-net-promoter-score-picker-button></lily-net-promoter-score-picker-button>
```

## Files

- `NetPromoterScorePickerButton.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `NetPromoterScorePickerButton.spec.ts` — vitest + TestBed render test
