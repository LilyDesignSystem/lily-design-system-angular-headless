# ColorPickerButton

a button showing a color swatch in a color picker

This is the Angular headless implementation. See `components/color-picker-button/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-color-picker-button></lily-color-picker-button>
```

## Files

- `ColorPickerButton.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ColorPickerButton.spec.ts` — vitest + TestBed render test
