# Loading

a loading indicator, such as text, or image, or animation

This is the Angular headless implementation. See `components/loading/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-loading></lily-loading>
```

## Files

- `Loading.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Loading.spec.ts` — vitest + TestBed render test
