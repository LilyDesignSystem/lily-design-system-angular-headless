# LatvijaPersonasKodsView

a read-only display of Latvia's Personas kods

This is the Angular headless implementation. See `components/latvija-personas-kods-view/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-latvija-personas-kods-view></lily-latvija-personas-kods-view>
```

## Files

- `LatvijaPersonasKodsView.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `LatvijaPersonasKodsView.spec.ts` — vitest + TestBed render test
