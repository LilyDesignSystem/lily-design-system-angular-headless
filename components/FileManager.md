# FileManager

a file browser for navigating and managing files

This is the Angular headless implementation. See `components/file-manager/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-file-manager></lily-file-manager>
```

## Files

- `FileManager.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `FileManager.spec.ts` — vitest + TestBed render test
