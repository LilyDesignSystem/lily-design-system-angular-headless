# PaginationNav

an ordered list of page navigation links

This is the Angular headless implementation. See `components/pagination-nav/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-pagination-nav></lily-pagination-nav>
```

## Files

- `PaginationNav.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `PaginationNav.spec.ts` — vitest + TestBed render test
