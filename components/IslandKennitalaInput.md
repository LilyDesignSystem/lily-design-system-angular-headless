# IslandKennitalaInput

an input for entering Iceland's Kennitala

This is the Angular headless implementation. See `components/island-kennitala-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-island-kennitala-input></lily-island-kennitala-input>
```

## Files

- `IslandKennitalaInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `IslandKennitalaInput.spec.ts` — vitest + TestBed render test
