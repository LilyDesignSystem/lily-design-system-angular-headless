# Label

a label associated with a form input

This is the Angular headless implementation. See `components/label/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-label></lily-label>
```

## Files

- `Label.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Label.spec.ts` — vitest + TestBed render test
