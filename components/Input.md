# Input

a generic HTML input element

This is the Angular headless implementation. See `components/input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-input></lily-input>
```

## Files

- `Input.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Input.spec.ts` — vitest + TestBed render test
