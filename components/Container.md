# Container

a generic block-level content container

This is the Angular headless implementation. See `components/container/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-container></lily-container>
```

## Files

- `Container.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Container.spec.ts` — vitest + TestBed render test
