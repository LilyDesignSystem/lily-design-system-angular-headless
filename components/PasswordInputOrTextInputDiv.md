# PasswordInputOrTextInputDiv

an input for entering a password <input type="password"> or text <input type="text"> with show/hide toggle

This is the Angular headless implementation. See `components/password-input-or-text-input-div/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-password-input-or-text-input-div></lily-password-input-or-text-input-div>
```

## Files

- `PasswordInputOrTextInputDiv.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `PasswordInputOrTextInputDiv.spec.ts` — vitest + TestBed render test
