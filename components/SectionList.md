# SectionList

a list of section navigation items

This is the Angular headless implementation. See `components/section-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-section-list></lily-section-list>
```

## Files

- `SectionList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `SectionList.spec.ts` — vitest + TestBed render test
