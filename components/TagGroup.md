# TagGroup

a group of tag elements

This is the Angular headless implementation. See `components/tag-group/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-tag-group></lily-tag-group>
```

## Files

- `TagGroup.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TagGroup.spec.ts` — vitest + TestBed render test
