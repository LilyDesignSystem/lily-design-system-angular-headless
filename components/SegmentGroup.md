# SegmentGroup

a group of mutually exclusive segment options

This is the Angular headless implementation. See `components/segment-group/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-segment-group></lily-segment-group>
```

## Files

- `SegmentGroup.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `SegmentGroup.spec.ts` — vitest + TestBed render test
