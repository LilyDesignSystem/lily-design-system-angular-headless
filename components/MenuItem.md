# MenuItem

one item in a menu

This is the Angular headless implementation. See `components/menu-item/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-menu-item></lily-menu-item>
```

## Files

- `MenuItem.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `MenuItem.spec.ts` — vitest + TestBed render test
