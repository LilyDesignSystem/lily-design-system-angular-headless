# TreeList

a hierarchical list with nested expandable items

This is the Angular headless implementation. See `components/tree-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-tree-list></lily-tree-list>
```

## Files

- `TreeList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TreeList.spec.ts` — vitest + TestBed render test
