# ChatList

an ordered list of chat list item components

This is the Angular headless implementation. See `components/chat-list/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-chat-list></lily-chat-list>
```

## Files

- `ChatList.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ChatList.spec.ts` — vitest + TestBed render test
