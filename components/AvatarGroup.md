# AvatarGroup

a group of avatar components

This is the Angular headless implementation. See `components/avatar-group/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-avatar-group></lily-avatar-group>
```

## Files

- `AvatarGroup.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `AvatarGroup.spec.ts` — vitest + TestBed render test
