# Character

a single character display element

This is the Angular headless implementation. See `components/character/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-character></lily-character>
```

## Files

- `Character.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Character.spec.ts` — vitest + TestBed render test
