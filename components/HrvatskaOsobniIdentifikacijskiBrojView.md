# HrvatskaOsobniIdentifikacijskiBrojView

a read-only display of Croatia's Osobni identifikacijski broj (OIB)

This is the Angular headless implementation. See `components/hrvatska-osobni-identifikacijski-broj-view/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-hrvatska-osobni-identifikacijski-broj-view></lily-hrvatska-osobni-identifikacijski-broj-view>
```

## Files

- `HrvatskaOsobniIdentifikacijskiBrojView.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `HrvatskaOsobniIdentifikacijskiBrojView.spec.ts` — vitest + TestBed render test
