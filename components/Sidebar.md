# Sidebar

a side panel for navigation or supplementary content

This is the Angular headless implementation. See `components/sidebar/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-sidebar></lily-sidebar>
```

## Files

- `Sidebar.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Sidebar.spec.ts` — vitest + TestBed render test
