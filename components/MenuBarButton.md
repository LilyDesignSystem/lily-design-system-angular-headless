# MenuBarButton

one item in a menu bar

This is the Angular headless implementation. See `components/menu-bar-button/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-menu-bar-button></lily-menu-bar-button>
```

## Files

- `MenuBarButton.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `MenuBarButton.spec.ts` — vitest + TestBed render test
