# ThemeView

a read-only display of the current theme

This is the Angular headless implementation. See `components/theme-view/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-theme-view></lily-theme-view>
```

## Files

- `ThemeView.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `ThemeView.spec.ts` — vitest + TestBed render test
