# Figure

a self-contained figure with optional caption

This is the Angular headless implementation. See `components/figure/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-figure></lily-figure>
```

## Files

- `Figure.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Figure.spec.ts` — vitest + TestBed render test
