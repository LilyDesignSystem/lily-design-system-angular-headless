# Panel

a generic content panel with optional heading

This is the Angular headless implementation. See `components/panel/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-panel></lily-panel>
```

## Files

- `Panel.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `Panel.spec.ts` — vitest + TestBed render test
