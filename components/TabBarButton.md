# TabBarButton

one tab button in a tab group

This is the Angular headless implementation. See `components/tab-bar-button/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-tab-bar-button></lily-tab-bar-button>
```

## Files

- `TabBarButton.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TabBarButton.spec.ts` — vitest + TestBed render test
