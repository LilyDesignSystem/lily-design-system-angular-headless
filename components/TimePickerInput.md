# TimePickerInput

an input with a dropdown for picking a time

This is the Angular headless implementation. See `components/time-picker-input/index.md`
in the canonical repo root for the cross-framework documentation.

## Selector

```html
<lily-time-picker-input></lily-time-picker-input>
```

## Files

- `TimePickerInput.ts` — standalone Angular 20 component (signal inputs, OnPush)
- `TimePickerInput.spec.ts` — vitest + TestBed render test
